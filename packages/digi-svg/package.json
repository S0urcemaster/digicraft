{
  "name": "@digicraft/svg",
  "version": "1.0.0",
  "main": "dist/svg",
  "types": "dist/svg",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "peerDependencies": {
    "react": "~18.2.0",
    "@types/react": "~18.2.0",
    "react-dom": "~18.2.0",
    "rimraf": "~5.0.0"
  },
  "dependencies": {
    "@digicraft/context": "*"
  },
  "devDependencies": {
  }
}
